package Incognito.utils;

import it.marteEngine.entity.Entity;

public class RayCasting {
	public static boolean Ray(Entity one, Entity two){
		System.out.println(one.getDistance(two));
		
		return false;
	}
}
